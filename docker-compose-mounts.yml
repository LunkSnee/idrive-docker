services:
  idrive:
    build: ./idrive/build/
    image: ghcr.io/lunksnee/idrive-docker:latest
    container_name: idrive
    volumes:
      - /path/to/config:/opt/IDriveForLinux/idriveIt
      - /path/to/backup:/source/1:ro
    restart: unless-stopped
    environment:
      - TZ=Etc/UTC

